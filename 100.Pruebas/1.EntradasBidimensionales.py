# Array Multidimensionales

data = [
    [1, 2, 3, 4, 3, 2, 1],
    [2, 2, 3, 4, 5, 6, 7]
        ]

# Lectura de array completo
# for i in range(len(data)):
#   for j in range(len(data[i])):
#       print(data[i][j])

print(data[0])  # Lectura de array en posicion 0
print(data[1])  # Lectura de array en posicion 1)
